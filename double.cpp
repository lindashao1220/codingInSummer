#include <iostream>
#include <math.h>
#include <string>

int main() {
    using namespace std;
    double x;
    x = sqrt(6.25);
    
    double answer;
    answer = pow(2, 2);

    cout << "Hello, " << x << "," << answer << "!" << std::endl;

    return 0;
}
